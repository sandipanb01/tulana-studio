fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
PyMuPDF>=1.24
Pillow>=10.0          # JPEG export of clippings
requests>=2.31        # fetching textbook archives from a link

# Setu's optional export formats. Without these the format is shown in the
# interface as unavailable, with the install command, rather than failing at
# the moment somebody clicks Download.
openpyxl>=3.1         # Setu: Excel (.xlsx) export
pyarrow>=14.0         # Setu: Parquet export

# Optional — the studio runs without these and says so.
# gradio       # only for share_gradio.py (public link)
# playwright   # only for browser testing
